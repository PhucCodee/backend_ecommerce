meta {
  name: User login
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "identifier": "goat",
    "password": "Phuc123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
